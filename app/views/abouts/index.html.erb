<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Sobre</h3>
  </div>
  <div class="panel-body">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>Título</th>
          <th>Conteúdo</th>
        </tr>
      </thead>

      <tbody>
        <% @abouts.each do |about| %>
          <tr>
            <td><%= about.title %></td>
            <td><%= raw(about.content) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div> <!-- End Panel -->
</div>
<br>

<% if @abouts.count > 0 %>
  <%= link_to 'Detalhes', @abouts.first %>
  <%= link_to 'Editar', edit_about_path(@abouts.first) %>
  <%= link_to 'Deletar', @abouts.first, method: :delete, data: { confirm: 'Are you sure?' } %>
<% else %>
  <%= link_to 'Incluir', new_about_path %>
<% end %>